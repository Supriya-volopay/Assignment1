import React from "react";

const Chart = ({xAxisData, yAxisData}) => {
    return(
        <>
        <h1>Chart</h1>
        {/* <div>
            {xAxisData.map((data) => (
                <h3>{data}</h3>
            ))}
        </div> */}
        </>
    );
}

export default Chart;